{%  extends 'base.html.twig' %}

{%  block title %} Insciption {% endblock %}
{%  block body %}
<div class="container mt-3">
    <h1>Inscrivez vous sur notre site!</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Configuration</button>
    {{ form_end(form) }}
</div>

{% endblock %}